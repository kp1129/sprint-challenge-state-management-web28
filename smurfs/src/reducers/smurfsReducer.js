// initial state
export const initialState = {
    message: "Add more Smurfs using the form below!",
    smurfs: []
}

// reducer function
export const smurfsReducer = (state, action) => {
    switch(action.type){
        default:
            return state;
    }
}